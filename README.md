# Telegram бот для автосалона AutoHouse

## Описание

Telegram бот для автосалона с каталогом автомобилей, системой фильтров и обработкой заявок от клиентов.

## Структура проекта

```
chatbot_auto/
├── main_new.py          # Главный файл запуска (рефакторинг)
├── main.py             # Старый монолитный файл
├── config.py           # Конфигурация и константы
├── utils.py            # Вспомогательные функции
├── keyboards.py        # Клавиатуры и меню
├── database.py         # Работа с данными автомобилей
├── handlers.py         # Обработчики команд и сообщений
├── admin.py            # Административные функции
├── requirements.txt    # Зависимости
├── data/
│   ├── datacars.json   # База данных автомобилей
│   └── photos/         # Папка для фотографий
└── README.md           # Документация
```

## Установка и запуск

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Настройка переменных окружения
Создайте файл `.env` в корне проекта:
```
BOT_TOKEN=ваш_токен_бота
ADMIN_IDS=ваш_telegram_id1,ваш_telegram_id2
```

**Примечание:** Можно указать несколько ID админов через запятую. Все они будут получать уведомления о новых заявках.

### 3. Запуск бота
```bash
python main_new.py
```

## Функциональность

### Для клиентов:
- 📋 Каталог автомобилей с фильтрами
- 🔍 Поиск по марке, типу кузова, двигателю, коробке передач, цене
- 📸 Просмотр фотографий автомобилей
- 📞 Оставление заявок на понравившиеся авто
- 💬 Получение контактов автосалона

### Для администраторов:
- 🔐 Админ-панель (`/admin`)
- ➕ Добавление новых автомобилей
- 🗑 Удаление автомобилей
- 📸 Управление фотографиями
- 📊 Просмотр списка автомобилей

## Архитектура

Проект разделен на модули для лучшей поддерживаемости:

- **config.py** - настройки и константы
- **utils.py** - вспомогательные функции
- **keyboards.py** - все клавиатуры и меню
- **database.py** - работа с JSON базой данных
- **handlers.py** - обработчики команд и callback-запросов
- **admin.py** - административные функции

## Разработка

### Добавление новых функций:
1. Определите логику в соответствующем модуле
2. Добавьте обработчики в `main_new.py`
3. Обновите клавиатуры в `keyboards.py` при необходимости

### Тестирование:
```bash
# Запуск с логированием
python main_new.py
```

## Контакты

Для вопросов по разработке пишите в Telegram: @vsfilippov

